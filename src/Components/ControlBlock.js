import { Grid } from '@mui/material';

function ControlBlock() {
    return (
      <Grid 
        container
        sx={{
          mb: 2
        }}
      >
        Control block
      </Grid>
    );
  }
  
  export default ControlBlock;